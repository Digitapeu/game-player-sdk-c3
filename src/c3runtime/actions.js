"use strict";

{
  C3.Plugins.DIGITAP_SDK.Acts = {
    SetProgress(state, score, level) {
      this.SetProgress(state, score, level);
    },
    SetLevelUp(level) {
      this.SetLevelUp(level);
    },
    SetPlayerFailed(state) {
      this.SetPlayerFailed(state);
    }
  };
}
